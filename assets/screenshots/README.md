# スクリーンショット置き場

LP やストア用のスクショをここに置く（ファイル名例: `01-home.png` … `05-result.png`）。
撮影は実機 or シミュレーター（横向き）から。アシスタントはシミュレーターを起動しないため、
キャプチャはユーザーが行う。必要サイズは `docs/store-listing.md` の「スクリーンショット」節を参照。

LP の `index.html` で使う場合は、ここに置いたうえで該当の `<img>` を差し込む。
